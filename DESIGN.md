# 大富翁网页版小游戏 — 设计文档

## 1. 项目概述

一个基于浏览器的大富翁（Monopoly）小游戏，支持真人多玩家联机与 AI 对战。
核心玩法围绕「掷骰子 → 移动 → 买地 → 收租 → 破产」循环展开。

## 2. 游戏规则说明

### 2.1 基本规则
- 游戏在经典 40 格棋盘上进行，玩家顺时针移动。
- 每位玩家初始资金 **$1500**。
- 每回合掷两颗骰子，按点数之和移动。
- 经过起点（GO）时领取 **$200**。

### 2.2 掷骰子与移动
- 掷出「双骰」（两颗点数相同）可**再掷一次**。
- 连续三次双骰视为「超速」，直接**进入监狱**。
- 后退类卡牌不触发经过起点奖励。

### 2.3 购买地产
- 停留在无人拥有的地产（街道/车站/公共事业）上且资金足够时，可选择**购买**。
- 购买后该地产归玩家所有；若放弃购买，则本轮不成交。

### 2.4 收取租金
- 停留在他人地产上时须支付租金，租金规则：
  - **街道**：基础租金 ×（垄断 ? 2 : 1）× **房屋倍率**。
    - 房屋倍率随数量递增且**无上限**：0 栋=1，1 栋=5，2 栋=12，3 栋=25，4 栋=40，之后每栋 +25。
    - 因此房价越高、房子越多的地产，能收取的租金越高。
  - **车站**：按拥有数量递增 `$25 / $50 / $100 / $200`。
  - **公共事业**：拥有一座时按 `4 × 骰子点数`，两座时按 `10 × 骰子点数`。

### 2.5 建造房屋（无上限）
- 玩家在**自己回合**可为其拥有的任何**街道地产**建造房屋，**数量无上限**（不再限 4 房 + 1 旅馆）。
- 建房屋数 = 1，花费该颜色分组的建房单价（棕色/浅蓝 $50、粉/橙 $100、红/黄 $150、绿 $200、深蓝 $200）。
- 建房只增加租金与净资产，不结束回合；玩家仍可继续掷骰/买卖。
- 车站与公共事业不能建房。

### 2.6 税、机会与社区福利
- **税格**：缴纳固定税款（所得税 $200、奢侈税 $100）。
- **机会 / 社区福利**：抽取卡牌，效果包括增减资金、前进/后退、前往起点、入狱、向他人收款等。

### 2.7 监狱
- 进入监狱的方式：走进「入狱」格、抽到入狱卡、连续三次双骰。
- 出狱方式：支付 **$50 保释金**，或掷出双骰。
- 最多尝试 3 次，第 3 次仍未掷出双骰将**强制保释**（资金不足则破产）。

### 2.8 破产与游戏结束
- 当玩家资金不足以支付租金/税款/保释金时**破产**：全部资产移交给债权人（或归还银行），退出游戏。
- 游戏结束条件（满足其一）：
  1. 仅剩一名未破产玩家 → 该玩家获胜；
  2. 达到最大行动次数（`maxTurns`）→ 净资产最高者获胜；
  3. **加时赛**：游戏时长超过阈值（默认 18 分钟）后进入加时赛，再完成约定回合数（默认 5 回合）仍未分出胜负 → 净资产最高者获胜。

### 2.9 加时赛机制
- 引擎在 `startGame` 记录开始时间 `startedAt`，每次回合推进检查用时。
- 用时超过 `config.overtimeMs`（默认 18 分钟）时置 `overtime = true`，并记录进入加时时的回合基准。
- 当已完成回合数与基准之差达到 `config.overtimeRounds`（默认 5）时，按 `netWorth`（现金 + 地产价值）判定获胜。
- 前端实时显示对局计时，加时赛中以金色高亮提示。

## 3. 界面布局设计

```
┌────────────────────────────────────────────────────────┐
│ 顶栏：标题「🎩 大富翁」   在线状态 · 房间号              │
├────────────────────────────────────────────────────────┤
│  大厅界面：昵称输入 · 创建房间 / 添加AI / 开始游戏        │
│            加入房间（输入房间号）· 玩家列表              │
├──────────────────────────────┬─────────────────────────┤
│                              │ 玩家状态面板             │
│       40 格棋盘（11×11）     │ 骰子显示（动画）         │
│       棋子实时位置           │ 操作按钮（掷/买/结束/保释）│
│       地产颜色分组/产权标记   │ 游戏日志（滚动）         │
└──────────────────────────────┴─────────────────────────┘
```

- **响应式**：桌面端为「棋盘 + 右侧栏」双列；窄屏（≤900px）纵向堆叠。
- **3D 化**：每个棋盘格采用渐变背景 + 内外阴影形成浮雕瓦片立体感；已拥有的街道地产随房屋数量渲染一排等距 3D 立方体建筑（CSS `transform-style: preserve-3d`），并显示房屋计数。
- **骰子动画**：掷骰时骰子旋转跳跃并快速轮换点数，客户端保证动画至少持续约 0.95s，结果返回后定格。加时赛中计时器闪烁提示。

## 4. 核心功能模块划分

| 模块 | 文件 | 职责 |
|------|------|------|
| 棋盘数据 | `src/board-data.js` | 40 格定义、卡牌、颜色分组、租金基准 |
| 工具函数 | `src/utils.js` | 掷骰、洗牌、随机 ID、金额格式化 |
| 游戏引擎 | `src/game-engine.js` | 核心规则：移动、购买、收租、破产、回合推进（纯逻辑、可测试） |
| 持久化 | `src/store.js` | JSON 文件原子读写 |
| 后端服务 | `app.js` / `server.js` | Express REST API、静态服务、AI 自动行动、多玩家同步 |
| 前端界面 | `index.html` / `style.css` / `game.js` | 棋盘渲染、玩家状态、骰子动画、操作交互、轮询同步 |

## 5. 架构设计

- **前端**：原生 HTML + CSS + JavaScript（ES Module），通过 REST API 与后端交互。
- **后端**：Node.js + Express，提供游戏状态管理、多玩家同步（轮询 + 版本号增量）、数据持久化。
- **游戏逻辑**：独立封装为 `src/game-engine.js` 纯模块，同时被前端、后端与测试复用。
- **AI**：后端在真人行动结束后自动为 AI 玩家执行掷骰/购买/保释，直至轮到真人。

## 6. REST API 设计

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/game` | 创建新游戏 |
| POST | `/api/game/:id/join` | 加入游戏（真人或 AI） |
| POST | `/api/game/:id/start` | 开始游戏 |
| GET | `/api/game/:id?v=N` | 查询状态（版本号增量，无变化返回 304） |
| POST | `/api/game/:id/roll` | 掷骰子 |
| POST | `/api/game/:id/buy` | 购买地产 |
| POST | `/api/game/:id/skip` | 放弃购买/结束行动 |
| POST | `/api/game/:id/bail` | 支付保释金 |

## 7. 已知限制与改进方向
- 未实现「建造房屋/旅馆」与「抵押贷款」，当前以垄断租金翻倍简化。
- 多浏览器场景下未做严格的身份鉴权，依靠房间号 + 本地令牌区分玩家。
- 可进一步引入 WebSocket/SSE 实现真正的实时推送，替换轮询。
